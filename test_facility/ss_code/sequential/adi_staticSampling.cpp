
 /* Start to analysis array index
Array index info: Total number of references: 34
p.addr (((i * 1024) + j) - 1)
u.addr ((j * 1024) + i)
u.addr (((j * 1024) + i) + 1)
v.addr (0 + i)
p.addr ((i * 1024) + 0)
v.addr (0 + i)
q.addr ((i * 1024) + 0)
v.addr (1047552 + i)
p.addr ((i * 1024) + j)
v.addr (((j + 1) * 1024) + i)
q.addr ((i * 1024) + j)
p.addr ((i * 1024) + j)
u.addr ((i * 1024) + 0)
u.addr (((j * 1024) + i) - 1)
q.addr ((i * 1024) + 0)
p.addr (((i * 1024) + j) - 1)
p.addr ((i * 1024) + j)
v.addr (((i - 1) * 1024) + j)
q.addr (((i * 1024) + j) - 1)
p.addr (((i * 1024) + j) - 1)
q.addr ((i * 1024) + j)
u.addr (((i * 1024) + j) + 1)
q.addr ((i * 1024) + j)
u.addr ((i * 1024) + j)
v.addr ((j * 1024) + i)
p.addr ((i * 1024) + 0)
u.addr ((i * 1024) + 0)
v.addr ((i * 1024) + j)
v.addr (((i + 1) * 1024) + j)
q.addr (((i * 1024) + j) - 1)
p.addr (((i * 1024) + j) - 1)
q.addr ((i * 1024) + j)
u.addr (((i * 1024) + 1024) - 1)
p.addr ((i * 1024) + j)

 Finish to analysis array index */ 

 /* Start to analyze argument
double* %p
double* %q
double* %v
double* %u

 Start to analysis argument */ 

 /* Start to analysis global variable 

 Finish to analysis global variable */ 

 /* Start analysis loops
--t
--Loop Bound: (1, 10)
--Loop inc: (t + 1)
--Loop predicate: <=
----i
----Loop Bound: (1, 1023)
----Loop inc: (i + 1)
----Loop predicate: <
------array access v.addr (0 + i)
------array access p.addr ((i * 1024) + 0)
------array access v.addr (0 + i)
------array access q.addr ((i * 1024) + 0)
------j
------Loop Bound: (1, 1023)
------Loop inc: (j + 1)
------Loop predicate: <
--------array access p.addr (((i * 1024) + j) - 1)
--------array access p.addr ((i * 1024) + j)
--------array access u.addr (((j * 1024) + i) - 1)
--------array access u.addr ((j * 1024) + i)
--------array access u.addr (((j * 1024) + i) + 1)
--------array access q.addr (((i * 1024) + j) - 1)
--------array access p.addr (((i * 1024) + j) - 1)
--------array access q.addr ((i * 1024) + j)
------array access v.addr (1047552 + i)
------j
------Loop Bound: (1022, 1)
------Loop inc: (j + -1)
------Loop predicate: >=
--------array access p.addr ((i * 1024) + j)
--------array access v.addr (((j + 1) * 1024) + i)
--------array access q.addr ((i * 1024) + j)
--------array access v.addr ((j * 1024) + i)
----i
----Loop Bound: (1, 1023)
----Loop inc: (i + 1)
----Loop predicate: <
------array access u.addr ((i * 1024) + 0)
------array access p.addr ((i * 1024) + 0)
------array access u.addr ((i * 1024) + 0)
------array access q.addr ((i * 1024) + 0)
------j
------Loop Bound: (1, 1023)
------Loop inc: (j + 1)
------Loop predicate: <
--------array access p.addr (((i * 1024) + j) - 1)
--------array access p.addr ((i * 1024) + j)
--------array access v.addr (((i - 1) * 1024) + j)
--------array access v.addr ((i * 1024) + j)
--------array access v.addr (((i + 1) * 1024) + j)
--------array access q.addr (((i * 1024) + j) - 1)
--------array access p.addr (((i * 1024) + j) - 1)
--------array access q.addr ((i * 1024) + j)
------array access u.addr (((i * 1024) + 1024) - 1)
------j
------Loop Bound: (1022, 1)
------Loop inc: (j + -1)
------Loop predicate: >=
--------array access p.addr ((i * 1024) + j)
--------array access u.addr (((i * 1024) + j) + 1)
--------array access q.addr ((i * 1024) + j)
--------array access u.addr ((i * 1024) + j)

Finish analysis loops */ 
 /* Start to analysis the number of samples
calculating:
Dump tree:
----Sample number: 10
------Sample number: 10220
--------Sample number: 10444840
--------Sample number: 10444840
------Sample number: 10220
--------Sample number: 10444840
--------Sample number: 10444840
 End of sample analysis */
 // Start to generating Static Sampling Code (reference based)
#include <map>
#include <set>
#include <vector>
#include <cstdlib>
#include <iostream>
#include <cmath>
#ifndef THREAD_NUM
#    define THREAD_NUM   4
#endif
#ifndef BIN_SIZE
#    define BIN_SIZE   4
#endif
#ifndef CHUNK_SIZE
#    define CHUNK_SIZE   4
#endif
using namespace std;
std::map<uint64_t, double> RT;
std::map<uint64_t, double> MR;
void rtHistoCal( int rt, int val ) {
    if ( val <= 0) {
;        return;
    }
    if (RT.find(rt) == RT.end()) { 
        RT[rt] = val;
    } else {
        RT[rt] += val;
    }
    return;
}
void subBlkRT(int rt) {
    int msb = 0;
    int tmp_rt = rt;
    while(tmp_rt != 0) {
        tmp_rt = tmp_rt / 2;
        ++msb;
    }
    if (msb >= BIN_SIZE) {
        int diff = (pow(2, msb) - pow(2, msb-1)) / BIN_SIZE;
        for (int b = pow(2, msb-1); b <= pow(2, msb); b+=diff) {
            if (rt < b) {
                rtHistoCal(b - diff, 1);
                break;
            }
        }
    }
    else {
        rtHistoCal(pow(2, msb-1), 1);
    }
    return;
}
void RTtoMR_AET() {
    std::map<uint64_t, double> P;
    double total_num_RT = 0;
    uint64_t max_RT = 0;
    for (std::map<uint64_t, double>::reverse_iterator it = RT.rbegin(), eit = RT.rend(); it != eit; ++it) {
        total_num_RT += it->second;
        if (max_RT < it->first) {
            max_RT = it->first;
        }
    }
    double accumulate_num_RT = 0;
    for (std::map<uint64_t, double>::reverse_iterator it = RT.rbegin(), eit = RT.rend(); it != eit; ++it) {
        P[it->first] = accumulate_num_RT / total_num_RT;
        accumulate_num_RT += it->second;
    }
    P[0] = 1;
    double sum_P = 0;
    uint64_t t = 0;
    uint64_t prev_t = 0;
    for (uint64_t c = 0; c <= max_RT && c <= 327680; c++) {
        while (sum_P < c && t <= max_RT) {
            if (P.find(t) != P.end()) {
                sum_P += P[t];
                prev_t = t;
            } else {
                sum_P += P[prev_t];
            }
            t++;
        }
        MR[c] = P[prev_t];
    }
    return;
}
void rtDump() {
    cout << "Start to dump reuse time histogram\n";
    for (map<uint64_t, double>::iterator it = RT.begin(), eit = RT.end(); it != eit; ++it) {
        cout << it->first << " " << it->second << "\n";
    }
    return;
}
void dumpMR() {
    cout << "miss ratio" << endl;
    std::map<uint64_t, double>::iterator it1 = MR.begin();
    std::map<uint64_t, double>::iterator it2 = MR.begin();
    while(it1 != MR.end()) {
        while(1) {
            std::map<uint64_t, double>::iterator it3 = it2;
            ++it3;
            if (it3 == MR.end()) {
                break;
            }
            if (it1->second - it3->second < 0.00001) {
                ++it2;
            } else {
                break;
            }
        }
        cout << it1->first << " " << it1->second << endl;
        if (it1 != it2) {
            cout << it2->first << " " << it2->second << endl;
        }
        it1 = ++it2;
        it2 = it1;
    }
    return;
}
/* v_addr (0 + i) 0 */
int calAddrv_addr0( int t, int i) {
    int result = ((0 + i)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + 0) 0 */
int calAddrp_addr0( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* v_addr (0 + i) 1 */
int calAddrv_addr1( int t, int i) {
    int result = ((0 + i)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + 0) 0 */
int calAddrq_addr0( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* p_addr (((i * 1024) + j) - 1) 1 */
int calAddrp_addr1( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + j) 2 */
int calAddrp_addr2( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* u_addr (((j * 1024) + i) - 1) 0 */
int calAddru_addr0( int t, int i, int j) {
    int result = ((((j * 1024) + i) - 1)) * 8 / 64;
    return result;
}
/* u_addr ((j * 1024) + i) 1 */
int calAddru_addr1( int t, int i, int j) {
    int result = (((j * 1024) + i)) * 8 / 64;
    return result;
}
/* u_addr (((j * 1024) + i) + 1) 2 */
int calAddru_addr2( int t, int i, int j) {
    int result = ((((j * 1024) + i) + 1)) * 8 / 64;
    return result;
}
/* q_addr (((i * 1024) + j) - 1) 1 */
int calAddrq_addr1( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* p_addr (((i * 1024) + j) - 1) 3 */
int calAddrp_addr3( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + j) 2 */
int calAddrq_addr2( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr (1047552 + i) 2 */
int calAddrv_addr2( int t, int i) {
    int result = ((1047552 + i)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + j) 4 */
int calAddrp_addr4( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr (((j + 1) * 1024) + i) 3 */
int calAddrv_addr3( int t, int i, int j) {
    int result = ((((j + 1) * 1024) + i)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + j) 3 */
int calAddrq_addr3( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr ((j * 1024) + i) 4 */
int calAddrv_addr4( int t, int i, int j) {
    int result = (((j * 1024) + i)) * 8 / 64;
    return result;
}
/* u_addr ((i * 1024) + 0) 3 */
int calAddru_addr3( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + 0) 5 */
int calAddrp_addr5( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* u_addr ((i * 1024) + 0) 4 */
int calAddru_addr4( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + 0) 4 */
int calAddrq_addr4( int t, int i) {
    int result = (((i * 1024) + 0)) * 8 / 64;
    return result;
}
/* p_addr (((i * 1024) + j) - 1) 6 */
int calAddrp_addr6( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + j) 7 */
int calAddrp_addr7( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr (((i - 1) * 1024) + j) 5 */
int calAddrv_addr5( int t, int i, int j) {
    int result = ((((i - 1) * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr ((i * 1024) + j) 6 */
int calAddrv_addr6( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* v_addr (((i + 1) * 1024) + j) 7 */
int calAddrv_addr7( int t, int i, int j) {
    int result = ((((i + 1) * 1024) + j)) * 8 / 64;
    return result;
}
/* q_addr (((i * 1024) + j) - 1) 5 */
int calAddrq_addr5( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* p_addr (((i * 1024) + j) - 1) 8 */
int calAddrp_addr8( int t, int i, int j) {
    int result = ((((i * 1024) + j) - 1)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + j) 6 */
int calAddrq_addr6( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* u_addr (((i * 1024) + 1024) - 1) 5 */
int calAddru_addr5( int t, int i) {
    int result = ((((i * 1024) + 1024) - 1)) * 8 / 64;
    return result;
}
/* p_addr ((i * 1024) + j) 9 */
int calAddrp_addr9( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* u_addr (((i * 1024) + j) + 1) 6 */
int calAddru_addr6( int t, int i, int j) {
    int result = ((((i * 1024) + j) + 1)) * 8 / 64;
    return result;
}
/* q_addr ((i * 1024) + j) 7 */
int calAddrq_addr7( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
/* u_addr ((i * 1024) + j) 7 */
int calAddru_addr7( int t, int i, int j) {
    int result = (((i * 1024) + j)) * 8 / 64;
    return result;
}
void ref_p_addr1() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr1]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr1() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr1]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr2() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr2]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr0() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr0]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr0() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr0]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr1() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr1]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr1(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr0() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr0]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr0(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr2() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr2]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr2(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr4() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr4]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB3 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB3 = j_Start;
                }
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr3() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr3]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB3 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB3 = j_Start;
                }
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr3() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr3]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB3 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB3 = j_Start;
                }
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr2() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr2]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr3() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr3]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr3(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr0() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr0]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 0 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr0(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr0(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 0 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr4() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr4]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr6() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr6]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr7() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr7]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr5() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr5]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr1() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr1]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 1 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr1(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr1(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 1 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr3() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr3]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 3 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr3(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr3(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 3 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr2() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr2]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB2 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB2 = j_Start;
                }
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 2 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr2(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr2(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 2 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr6() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr6]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB6 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB6 = j_Start;
                }
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr7() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr7]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB6 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB6 = j_Start;
                }
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr7() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr7]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB6 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB6 = j_Start;
                }
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr4() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr4]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB1 = 1;
        int jLB3 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB1 = i_Start;
            }
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB3 = j_Start;
                }
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr4(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr5() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr5]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr4() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr4]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 4 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr4(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr4(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 4 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr6() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr6]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_v_addr7() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[v_addr7]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr0( it->at(0), it->at(1)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr1( it->at(0), it->at(1)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 1 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrv_addr2( it->at(0), it->at(1)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 7 refNumber[LoopRefTree->AA]: 2 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr3( it->at(0), it->at(1), it->at(2)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr4( it->at(0), it->at(1), it->at(2)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 4 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr5( it->at(0), it->at(1), it->at(2)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr6( it->at(0), it->at(1), it->at(2)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrv_addr7( it->at(0), it->at(1), it->at(2)) == calAddrv_addr7(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrv_addr7(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 7 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr5() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr5]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr5(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr8() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr8]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 8 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 8 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr8(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr8(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 8 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_q_addr6() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[q_addr6]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB5 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr0( it->at(0), it->at(1)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr1( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr2( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 2 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr3( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrq_addr4( it->at(0), it->at(1)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 6 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB5 = j_Start;
                }
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr5( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 5 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr6( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 6 */
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrq_addr7( it->at(0), it->at(1), it->at(2)) == calAddrq_addr6(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrq_addr6(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 6 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_u_addr5() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10220;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[u_addr5]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 2  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr0( it->at(0), it->at(1), it->at(2)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 0 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr1( it->at(0), it->at(1), it->at(2)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr2( it->at(0), it->at(1), it->at(2)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                vector<int> v = { t, i};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr3( it->at(0), it->at(1)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 3 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr4( it->at(0), it->at(1)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 4 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddru_addr5( it->at(0), it->at(1)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                    if (it->at(0) == t_Start && it->at(1) == i_Start                ) { cntStart = true; }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 5 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ")" << endl;
#endif
                    vector<int> v = { t, i};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr6( it->at(0), it->at(1), it->at(2)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddru_addr7( it->at(0), it->at(1), it->at(2)) == calAddru_addr5(t_Start, i_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddru_addr5(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 5 refNumber[LoopRefTree->AA]: 7 */
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
void ref_p_addr9() {
    /* Generating sampling loop */
    set<string> record;
    for ( int s = 0; s < 10444840;) {
SAMPLE:
        int t_Start = rand() % (10 - 1 + 1) + 1;
        if ( (1023 - 1) == 0) goto SAMPLE;
        int i_Start = rand() % (1023 - 1) + 1;
        if ( (1 - 1022 + 1) == 0) goto SAMPLE;
        int j_Start = rand() % (1022 - 1 + 1) + 1;
        string idx_string = std::to_string(t_Start) + "_" + std::to_string(i_Start) + "_" + std::to_string(j_Start) + "_" ;
        if ( record.find(idx_string) != record.end() ) goto SAMPLE;
        record.insert( idx_string );
#ifdef DEBUG
        cout << "[p_addr9]Samples: " << idx_string << endl;
#endif
        uint64_t cnt = 0;
        bool cntStart = false;

        /* Variable used to compute thread-local iteration space */
        int B = 0;
        auto BLIST = new int[THREAD_NUM][2];
        int seperator = 0;
        int t_Start = 0;
        /* Generating reuse search code */
        /* Sampled IDVs 3  */
        /* Sampled IDV: t  */
        /* Sampled IDV: i  */
        /* Sampled IDV: j  */
        int tLB0 = t_Start;
        int iLB4 = 1;
        int jLB6 = 1022;
        /* Vector that contains the interleaved iteration, avoid duplicate declaration */
        vector<vector<int>> nv(THREAD_NUM);
        /* Generating thread local iteration space mapping code */
#ifdef DEBUG
        cout << "Count: " << cnt << endl;
#endif
        /* Compute the chunk size. */
#ifdef CHUNK_SIZE
        int chunk_size = CHUNK_SIZE;
        int chunk_num = (10 - 1) / (THREAD_NUM * chunk_size);
#else
        int chunk_size = (10 - 1) / THREAD_NUM;
        int chunk_num = 1;
#endif
        /* Compute the number of chunks */
        int c_Start = (i_Start - 1) / (THREAD_NUM * chunk_size);
        cout << "c_Start = " << c_Start << ", chunk_num = " << chunk_num << endl;
        for (int cid = c_Start; cid < chunk_num; cid++) {
            /* Computes bound express for each thread */
            for (int t = 0; t < THREAD_NUM; ++t) {
                BLIST[t][0] =  1+ (cid * THREAD_NUM + t) * chunk_size;
                BLIST[t][1] = min(1 + (cid * THREAD_NUM + t + 1) * chunk_size, 10) - 1;
#ifdef DEBUG
                cout << "[Thread " << t << "], " << "(" << BLIST[t][0] << ", "<< BLIST[t][1] << ")" << endl;
#endif
            }
            /* Iterate within a chunk */
            int ci_Start = 0;
            if (cid == c_Start) {
                ci_Start = (i_Start - 1) % chunk_size;
            }
            for ( int ci = ci_Start; ci < chunk_size; ci++) {
                if ( cid != c_Start || ci != ci_Start ) {
                    tLB0 = cid * (THREAD_NUM * chunk_size) + ci;
                }
            /* Generating thread local iteration space mapping code */
            for ( int i = iLB1; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr0( it->at(0), it->at(1)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 9 refNumber[LoopRefTree->AA]: 0 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr1]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr0]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB2; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr1( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 1 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr2( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 2 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr0]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr1]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr3( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 3 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr2]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[v_addr2]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB3; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr4( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 4 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr3]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr4]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            /* Generating thread local iteration space mapping code */
                if ( tLB0 == t_Start ) {
                iLB4 = i_Start;
            }
            for ( int i = iLB4; i < 1023; i++) {
                int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                vector<int> v = { t, i, j};
                /* Interleaving */
                t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                cout << "Generate interleaved iteration for (";
                for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                    cout << *it;
                    if (it != v.end()) { cout << ", "; }
                }
                cout << ")" << endl;
#endif
                for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                    vector<int> tmp;
                    for (int vi = 0; vi < v.size(); vi++ ) {
                        if (vi == 0) {
                            tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                        } else {
                            tmp.push_back(v[vi]);
                        }
                    }
                    if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                    cout << "(";
                    for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                        cout << *it << ", ";
                    }
                    cout << ")" << endl;
#endif
                }
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr3]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    /* Remove those invalid interleaving */
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[p_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                        if ( calAddrp_addr5( it->at(0), it->at(1)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                            cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1)) << ", " << "(" << it->at(0) << ", " << it->at(1) << "), " << cnt << ") " << endl;
                            rtHistoCal(cnt, 1);
#else
                            subBlkRT(cnt);
#endif
                            goto EndSample;
                        }
                    }
                }
#ifdef DEBUG
                cout << endl;
                /* useID: 9 refNumber[LoopRefTree->AA]: 5 */
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[q_addr4]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                for ( int j = jLB5; j < 1023; j++) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr6( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 6 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr7( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 7 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[v_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr5]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr8]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr8( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 8 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
                /* Generating thread local iteration space mapping code */
                /* iterate thread local iteration space mapping code after interleaving */
                for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                    if (it->size() <= 0) { continue; }
                    if (cntStart == true) {
                        cnt++;
#ifdef DEBUG
                        cout  << "[u_addr5]" << it->at(0) << ", " << it->at(1) << ", cnt: " << cnt << ")	";
#endif
                    }
                } // end of interleaving loop
#ifdef DEBUG
                cout << endl;
#endif
                /* Generating thread local iteration space mapping code */
                    if ( tLB0 == t_Start && iLB1 == i_Start ) {
                    jLB6 = j_Start;
                }
                for ( int j = jLB6; j >= 1; j--) {
                    int t = cid * (THREAD_NUM * chunk_size) + ci + 1;
#ifdef DEBUG
                    cout << "Iterate (" << t << ", " << i << ", " << j << ")" << endl;
#endif
                    vector<int> v = { t, i, j};
                    /* Interleaving */
                    t_Start = ((t - 1) / chunk_size) % THREAD_NUM;
#ifdef DEBUG
                    cout << "Generate interleaved iteration for (";
                    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
                        cout << *it;
                        if (it != v.end()) { cout << ", "; }
                    }
                    cout << ")" << endl;
#endif
                    for ( int tid = t_Start; tid < THREAD_NUM; tid++) {
                        vector<int> tmp;
                        for (int vi = 0; vi < v.size(); vi++ ) {
                            if (vi == 0) {
                                tmp.push_back(v[0] + chunk_size * (tid - t_Start));
                            } else {
                                tmp.push_back(v[vi]);
                            }
                        }
                        if (tmp.size() > 0) { nv[tid] = tmp; }
#ifdef DEBUG
                        cout << "(";
                        for (vector<int>::iterator it = nv[tid].begin(); it != nv[tid].end(); it++) {
                            cout << *it << ", ";
                        }
                        cout << ")" << endl;
#endif
                    }
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        /* Remove those invalid interleaving */
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[p_addr9]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                            if ( calAddrp_addr9( it->at(0), it->at(1), it->at(2)) == calAddrp_addr9(t_Start, i_Start, j_Start)) {
#ifdef DEBUG
                                cout << "[REUSE FIND] @ (" << calAddrp_addr9(it->at(0), it->at(1), it->at(2)) << ", " << "(" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << "), " << cnt << ") " << endl;
                                rtHistoCal(cnt, 1);
#else
                                subBlkRT(cnt);
#endif
                                goto EndSample;
                            }
                        }
                        if (it->at(0) == t_Start && it->at(1) == i_Start && it->at(2) == j_Start                    ) { cntStart = true; }
                    }
#ifdef DEBUG
                    cout << endl;
                    /* useID: 9 refNumber[LoopRefTree->AA]: 9 */
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr6]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[q_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                    /* Generating thread local iteration space mapping code */
                    /* iterate thread local iteration space mapping code after interleaving */
                    for (vector<vector<int>>::iterator it = nv.begin(); it != nv.end(); it++) {
                        if (it->size() <= 0) { continue; }
                        if (cntStart == true) {
                            cnt++;
#ifdef DEBUG
                            cout  << "[u_addr7]" << it->at(0) << ", " << it->at(1) << ", " << it->at(2) << ", cnt: " << cnt << ")	";
#endif
                        }
                    } // end of interleaving loop
#ifdef DEBUG
                    cout << endl;
#endif
                } // end of inner for loops
            } // end of inner for loops
            } // end of outer for - ci loops
        } // end of outer for - cid loops
EndSample:
        s++;
        }
}
int main() {
    ref_p_addr1();
    ref_u_addr1();
    ref_u_addr2();
    ref_v_addr0();
    ref_p_addr0();
    ref_v_addr1();
    ref_q_addr0();
    ref_v_addr2();
    ref_p_addr4();
    ref_v_addr3();
    ref_q_addr3();
    ref_p_addr2();
    ref_u_addr3();
    ref_u_addr0();
    ref_q_addr4();
    ref_p_addr6();
    ref_p_addr7();
    ref_v_addr5();
    ref_q_addr1();
    ref_p_addr3();
    ref_q_addr2();
    ref_u_addr6();
    ref_q_addr7();
    ref_u_addr7();
    ref_v_addr4();
    ref_p_addr5();
    ref_u_addr4();
    ref_v_addr6();
    ref_v_addr7();
    ref_q_addr5();
    ref_p_addr8();
    ref_q_addr6();
    ref_u_addr5();
    ref_p_addr9();
    rtDump();
    RTtoMR_AET();
    dumpMR();
    return 0;
}
 /* Analyze function: adi */ 
